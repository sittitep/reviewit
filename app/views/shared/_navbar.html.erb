<nav class="navbar navbar-expand-lg navbar-light bg-white">
  <div class="container">
    <%= link_to "reviewit", root_path, class: "navbar-brand" %>
    <% if current_user %>
      <div class="ml-auto">
        <span class="mr-2"><%= current_user.name %></span><%= image_tag(current_user.avatar_url, width: 32, class: "rounded-circle") %>
      </div>
    <% else %>
      <%= link_to "Login with GitHub", Github.authentication_url, class: "btn btn-outline-success ml-auto" %>
    <% end %>
  </div>
</nav>
