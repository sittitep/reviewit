<nav class="navbar navbar-expand-lg navbar-light bg-white">
  <div class="container">
    <%= link_to "reviewit", root_path, class: "navbar-brand" %>
    <% if current_user %>
      <div class="ml-auto d-flex align-items-center">
        </span><%= image_tag(current_user.avatar_url, width: 32, class: "rounded-circle") %>
        <span class="text-muted d-none d-md-inline ml-2"><%= current_user.name %></span>
        <%= link_to "Request a Review", new_post_path, class: "mr-4 btn-outline-primary btn ml-4"  %>
        <%= link_to "Logout", sessions_path, method: :delete, class: "text-muted" %>
      </div>
    <% else %>
      <%= link_to "Login with GitHub", Github.authentication_url, class: "btn btn-outline-success ml-auto" %>
    <% end %>
  </div>
</nav>
